1.数据库sql优化的几种方法
	1.1 执行计划
	1.2 写统一的SQL语句
	1.3 不要把SQL语句写的太长，太过冗余，子查询嵌套不要超过三层
	1.4 中间结果暂存在临时表中，可以减少程序执行中“共享锁”阻塞“更新锁”，提高
		并发性能
	1.5 联机事物处理系统中，sql语句必须采用绑定变量，以免SQL重复解析
	1.6只在必要的情况下使用不begin tran,begin tran 套住的SQL过多时
		所有SQL语句封住的资源都不能释放，直到commit掉